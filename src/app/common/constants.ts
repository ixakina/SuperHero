export enum LocStorKeys {
  CURRENT_USER_ID = 'currentUserId',
  USERS = 'users'
}

export enum SortType {
  ASK = 'asc',
  DESC = 'desk'
}

export  enum SortCase {
  ALPHABET='alphabet',
  DATE = 'date'
}
